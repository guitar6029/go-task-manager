events {
    #
}

http {
    resolver 127.0.0.11;

    upstream app_backend {
        server app:8080;
        server app2:8080;
    }

    server {
        listen 80;

        location / {
            proxy_pass http://app_backend;
        }
    }
}